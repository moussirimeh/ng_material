export class Dcom {
      num: string;
      combine: string;
      code: string;
      quantite: string;
      tRemise: string;
      prix: string;
      tauxTva: string;
      livre: string;
      operateur: string;
      ref: string;
      qteStk: string;
      qteASatisfaire: string;
      qteEnCoursCmd: string;
	  qtReserv: string;
}
